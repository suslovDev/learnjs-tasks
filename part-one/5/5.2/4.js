let i = 0;
while (i != 10) {
  i += 0.2;
}
/* Из-за того как JS работает с числами, 
   при работе с  такими числами как 0.2, 
   например, происходит потеря точности. 
   Поэтому i никогда не станет = 10.
   Этот цикл не завершится*/
